# Installed with:
#   pip install --index-url https://download.pytorch.org/whl/cu126 \
#               --extra-index-url https://pypi.org/simple -r requirements.txt
# The PyTorch wheel index takes priority for torch/torchaudio (gives the
# CUDA 12.6 build); everything else resolves from PyPI via the extra index.
#
# cu126, not cu124: bumped alongside torch/torchaudio 2.6.0 -> 2.11.0.
# cu126 has full torch+torchaudio cp313 coverage for the 2.11.0 release
# (both packages have shipped cp313 wheels on every CUDA channel since
# 2.6.0 -- see the 0.1.4 history below -- so this bump carries no repeat
# of that cp313-coverage problem).
#
# torch==2.11.0 / torchaudio==2.11.0, not 2.6.0: version bump per request;
# melband-roformer-infer==0.1.5 only requires torch>=2.0, so this is
# compatible with no source/CLI code changes needed.
#
# (Historical note, resolved by the above: torchaudio never published ANY
# cp313 wheel at all for the 2.5.x series, on any index -- checked against
# PyPI's file listing for the torchaudio 2.5.1 release, which only goes up
# to cp312. torchaudio's first cp313 wheels shipped with the 2.6.0
# release. torch itself did publish a 2.5.1 cp313 wheel, which is why only
# the torchaudio half of `pip install -r requirements.txt` failed back
# then.)
#
# CUDA 12.6 build requires NVIDIA driver >= 560.28.03 (CUDA 12.6 GA linux
# floor). RTX 3060 Ti is Ampere/sm_86, supported by every current PyTorch
# CUDA build (no architecture concern here, only the driver-version floor
# above).
#
# NOTE: this driver floor is also checked at runtime by
# app/melband_roformer_wrapper/gpuinfo.py (TORCH_CUDA_CHANNEL /
# MIN_DRIVER_VERSION). If you change the wheel index here or in
# debian/rules' TORCH_INDEX, update those two constants in gpuinfo.py in
# the same commit -- it is the single place the runtime --info/--self-test
# checks read from.
torch==2.11.0
torchaudio==2.11.0

# Real upstream project this package wraps. Pinned to the version verified
# against this README/CLI at packaging time (see README.debian.md).
melband-roformer-infer==0.1.5

soundfile>=0.12,<0.13
