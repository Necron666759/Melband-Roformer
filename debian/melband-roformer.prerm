#!/bin/sh
set -e

# Nothing custom needed: everything this package owns lives under paths
# dpkg already tracks and removes itself (/usr/lib/melband-roformer,
# /usr/bin/melband-roformer*, /usr/share/{applications,icons,man,doc}/...).
# We intentionally do not touch the user's model cache
# (~/.cache/melband-roformer-infer/) from a root maintainer script.

#DEBHELPER#

exit 0
